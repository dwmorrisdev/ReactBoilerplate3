var React = require('react');

var Timer = React.createClass({
  render: function () {
    return (
    	<div className="holder">
    		<p>Timer component</p>
    	</div>
    )
  }
});

module.exports = Timer;